# MESSAGE: mfact PaaS profile must declare PROV-O conformance
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?violation WHERE {
  BIND("conforms_prov_o" AS ?violation)
  FILTER NOT EXISTS { <https://mfact.dev/paas> dcterms:conformsTo <https://www.w3.org/TR/prov-o/> . }
}
