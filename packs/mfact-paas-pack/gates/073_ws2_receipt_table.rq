# MESSAGE: receipt shape must preserve Postgres table binding
PREFIX mfp: <https://mfact.dev/paas#>
SELECT ?v WHERE { BIND("receipt_table" AS ?v) FILTER NOT EXISTS { mfp:ReceiptShape mfp:table "mfact_receipts" . } }
