# MESSAGE: mfact PaaS profile must declare ODRL conformance
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?violation WHERE {
  BIND("conforms_odrl" AS ?violation)
  FILTER NOT EXISTS { <https://mfact.dev/paas> dcterms:conformsTo <https://www.w3.org/TR/odrl-model/> . }
}
