% RENDERED by `ggen sync` from the post-release graph (post-release-pack) — DO NOT EDIT BY HAND
\paragraph{Publication packet.}
Publication of v26.7.6 is manufactured as its own identity, distinct
from the certified core release: a packet hash
(\texttt{\footnotesize 9c787499b845d2dcbe4212ac416f3469f0849b187bfcf80b21bf60d2313ada29})
folded over the packet's evidence inputs, with each actuation surface
gated on evaluated requirements. The arXiv packet is
\texttt{ALIVE} (cold build PASS from untarred package (13 declared files);
13 declared files, declared $=$ packed enforced).
Packet statuses:
\texttt{arxiv\_upload}$=$\texttt{ALIVE}, \texttt{github\_push}$=$\texttt{BLOCKED}, \texttt{github\_release}$=$\texttt{ALIVE}.
No packet self-actuates: publication is recorded as
\texttt{PENDING\_EXTERNAL\_ACTUATION} in every case, and the
kernel-admitted witness \texttt{ProcInt.Release.PostRelease} refuses any
render in which a packet claims otherwise.

