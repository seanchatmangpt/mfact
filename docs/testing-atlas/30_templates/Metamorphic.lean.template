def transform : X → X := ...
def observe : X → Y := ...
def LawfulTransform : X → Prop := ...

theorem transformed_observable_preserved
    (x : X)
    (h : LawfulTransform x) :
    observe (transform x) = observe x := by
  ...
