# Expected-Failure Fixture

The harness must verify all three:

1. Lean/Lake exits nonzero.
2. The intended diagnostic class/message occurs.
3. The failure is not caused by import/path/parse damage unrelated to the law.

Fixture instance ID: `MFW.TST.EXPECTED_FAIL.BUILD.___`
